# Documentation

This folder contains copied project documentation so the repository is organized into separate areas for backend, frontend, and docs.

- Root app logic lives in the project package modules.
- Backend entrypoint: `backend/run.py`
- Frontend app scaffold: `frontend/`
